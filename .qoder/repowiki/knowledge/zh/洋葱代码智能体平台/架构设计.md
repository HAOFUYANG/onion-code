- 根模块通过 `src/agent/cli.ts` 作为唯一启动入口，利用 `commander` 解析命令参数，根据子命令（`ask` 或默认）动态路由至 `agent_core` 的逻辑流或 `ink_ui` 的 React 渲染树。
- 共享 `package.json` 中的依赖项（如 `langchain`、`ink`、`react`）和 `tsconfig.json` 编译配置，确保两个子模块在 TypeScript 类型系统与运行时环境上的一致性。
- 通过统一的构建脚本 `build` 将源码编译至 `dist` 目录并同步技能文件，实现核心逻辑与界面资源的原子化打包与分发。
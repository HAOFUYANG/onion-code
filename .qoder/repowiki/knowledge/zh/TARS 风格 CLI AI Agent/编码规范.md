- 所有工具函数均使用 `@langchain/core/tools` 的 `tool` 包装器进行定义，并通过 `zod` 声明强类型输入 Schema。
- 技能模块遵循标准化结构，每个子目录必须包含 `SKILL.md`，且以 YAML frontmatter 定义 `name` 和 `description`。
- 涉及文件系统或进程操作的工具（如 `execTool`）均实现了黑名单过滤与正则模式匹配的双重安全校验逻辑。
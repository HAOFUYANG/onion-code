- 所有工具模块均成对包含实现文件（.ts）与测试文件（.test.ts），并使用 Vitest 进行单元测试。
- 技能（Skills）遵循目录即配置的约定，每个子目录必须包含 `SKILL.md` 且通过 YAML frontmatter 声明元数据。
- 系统提示词（System Prompt）采用模块化拼接，将静态人设（TARS 风格）与动态发现的技能列表（`getSkillText`）合并注入。
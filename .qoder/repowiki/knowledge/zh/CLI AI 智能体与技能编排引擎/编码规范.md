- 所有工具函数均遵循 LangChain Tool 规范，使用 `tool()` 包装器定义，并配合 `zod` schema 明确输入结构。
- 技能描述文件统一采用 `SKILL.md` 命名，并通过 YAML frontmatter（name, description）实现元数据标准化。
- 模块内部路径引用优先使用 `path.resolve(__dirname, ...)` 确保在编译后的 `dist` 目录或源码 `src` 目录下均能正确定位资源。
- 采用四层架构设计：底层为 RuntimeCore（内部状态管理），向上依次为 Runtime（公共 API）、Context Hooks（React 上下文钩子）和 Primitives（UI 原语组件）。
- 核心入口为 `SKILL.md`，通过引用 `references/architecture.md` 和 `references/packages.md` 分别阐述分层模型与包生态。
- 依赖方向严格自下而上：UI 组件依赖 Context Hooks，Hooks 依赖 Runtime 层，Runtime 层适配后端适配器（如 AI SDK、LangGraph）。